# ── 런타임 (엔진 동작에 필수) ────────────────────────────────
PyYAML>=6.0            # 룰 DB · 촬영 프로토콜 · 실측 SOP 로드
numpy>=1.24            # 마커 호모그래피 선형 방정식

# ── 촬영물 분석 ──────────────────────────────────────────────
opencv-python>=4.8     # ArUco 마커 검출 · 영상 휘도 추출 · 마커 카드 생성
Pillow>=10.0           # 문자 높이 계수 교정 (폰트 렌더·잉크 박스 측정)

# ── 선택 — OCR (별표5 3.g 문자 높이) ────────────────────────
# 없어도 엔진은 동작한다. 없으면 3.g 가 실측으로 넘어갈 뿐이다.
# 전부 로컬에서 돌고 사진이 밖으로 나가지 않는다.
#
#   pip install rapidocr        # 한글 지원 (PP-OCRv5 korean). torch 불필요
#
# 구 패키지 rapidocr-onnxruntime 은 쓰지 말 것 — 딸려오는 모델이
# 중국어·영문 전용이라 **한글을 한 글자도 읽지 못한다.**
# 설치한 뒤 반드시 한 번: KFA.exe calibrate-ocr
#   (최초 실행 시 인식 모델 약 18MB 를 내려받는다)
rapidocr>=3.4            # 배포 EXE에도 PP-OCRv5 korean 모델과 함께 포함

# ── 선택 — L2 멀티모달 판정 (별표5 25항목) ───────────────────
# 없어도 엔진은 동작한다. 없으면 해당 항목이 '검토자'로 넘어갈 뿐이다.
# 실행 파일에는 동봉하지 않는다 — 쓰는 사람이 필요할 때 넣는다.
#   pip install anthropic
#   set ANTHROPIC_API_KEY=...   /   set KFA_L2_PROVIDER=anthropic
anthropic>=0.40          # 조작판에서 API 키를 넣으면 L2 위반 후보 선별 사용

# ── 개발 ────────────────────────────────────────────────────
pytest>=8.0
