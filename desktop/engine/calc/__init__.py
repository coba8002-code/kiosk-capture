"""L1 결정론적 계측 — 코드가 계산하는 층.

같은 입력이면 같은 값이 나오고, 리포트에 수식과 측정값이 그대로 실린다.
법적 방어력은 이 층에서 나온다. LLM 은 여기에 관여하지 않는다.
"""
from . import contrast, flash_rate, geometry, scale, sound, text_height  # noqa: F401
